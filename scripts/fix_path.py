import sys
from pathlib import Path

sys.path.append(str(Path(__file__).parent.parent))

# TODO: is there any way to activate beartype?
# Or should we just disallow this kind of script running.
# In that case an alternative needs to be provided.
